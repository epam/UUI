# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ cicd_test ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build-and-test:
    
    runs-on: ubuntu-latest
    container: node:14
  

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '14.x'
          
      # - name: Get yarn cache directory path
        # id: yarn-cache-dir-path
        # run: echo "::set-output name=dir::$(yarn config get cacheFolder)"
        # check commit 
        
      # - name: Cache yarn dependencies
        # uses: actions/cache@v2
        # id: cache1
        # with:
          # path: |
            # node_modules
          # key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          # restore-keys: |
            # ${{ runner.os }}-yarn-


       
# */*/node_modules
      # - name: Cache npm dependencies
        # uses: actions/cache@v2
        # with:
          # path: '~/.npm'
          # key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          # restore-keys: |
            # ${{ runner.os }}-node-   

      - uses: actions/upload-artifact@v2
        with:
          name: my-artifact
          path: |
            app/
            public/
            server/
            changelog.md
            package.json
      
            
      # - uses: actions/cache@v2
        # with:
          # path: |
            # app/
            # public/
            # server/
            # changelog.md
            # package.json
          # key: test1
          # key: v1-app-deps-${{ hashFiles('**/package.json') }}
          # restore-keys: |
            # v1-app-deps-

      - name: build app
        run: | 
          yarn
          yarn build
          yarn generate-components-api
          cd server && yarn
          
      - name: check cache key
        run: |
          ls -ln
          echo "${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}"
          echo "v1-app-deps-${{ hashFiles('**/package.json') }}"
     
        
      - name: test  
        run: yarn test
        
  docker:
    needs: build-and-test
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ui
      ACTIONS_STEP_DEBUG: true
      REGISTRY_URL: epmtai.azurecr.io
    steps:
      - name: Check Out Repo 
        uses: actions/checkout@v2
      
      # - name: Get yarn cache directory path
        # id: yarn-cache-dir-path
        # run: echo "::set-output name=dir::$(yarn config get cacheFolder)"

      # - name: Cache yarn dependencies
        # id: cache1
        # uses: actions/cache@v2
        # with:
          # path: |
            # node_modules
          # key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          # restore-keys: |
            # ${{ runner.os }}-yarn-

#            */*/node_modules
      # - name: Cache npm dependencies
        # uses: actions/cache@v2
        # with:
          # path: '~/.npm'
          # key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          # restore-keys: |
            # ${{ runner.os }}-node-   
            
      # - uses: actions/cache@v2
        # with:
          # path: |
            # app/
            # public/
            # server/
            # changelog.md
            # package.json
          # # key: v1-app-deps-${{ hashFiles('**/package.json') }}
          # key: test1
          # restore-keys: |
            # v1-app-deps-

      - uses: actions/download-artifact@v2
        with:
          name: my-artifact
      
            
      - name: check cache key
        run: |
          ls -l app/
          ls -l server/
          echo $GITHUB_SHA
# echo "${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}"
# echo "v1-app-deps-${{ hashFiles('**/package.json') }}"

      # - name: install dependencies
        # run: | 
          # cd app && yarn 
          # cd ../server && yarn
          
      - name: Login to registry
        uses: docker/login-action@v1 
        with:
          registry: ${{ secrets.REGISTRY_URL }}
          username: ${{ secrets.REGISTRY_USER }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
        
      - name: ls
        run: ls -la
        
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.REGISTRY_URL }}/ui:0.0.2223
          
        
        
        
        
        
        
