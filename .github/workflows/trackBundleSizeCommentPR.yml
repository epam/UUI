name: track_bundle_size_comment_pr

on:
    workflow_run:
        workflows: [track_bundle_size]
        types: [completed]

permissions:
    pull-requests: write

jobs:
    track_bundle_size_comment_pr:
        runs-on: ubuntu-latest
        steps:
            - name: 'Download trackBundleSize.md and prNumber.txt'
              uses: actions/github-script@v6
              with:
                  script: |
                      console.log(JSON.stringify(github.event.workflow_run, undefined, 2))
